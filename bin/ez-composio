#!/bin/sh
set -eu
: "${EZ_COMPOSIO_LAUNCHER:?Use the agent-bound tools/bin/ez or set its absolute path}"
case "$EZ_COMPOSIO_LAUNCHER" in /*) ;; *) exit 2;; esac
exec "$EZ_COMPOSIO_LAUNCHER" composio "$@"
